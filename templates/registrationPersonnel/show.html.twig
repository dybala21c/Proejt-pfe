{% extends 'base_admin.html.twig' %}

{% block title %}Personnel{% endblock %}

{% block body %}
    <h1>Personnel</h1>

    <table class="table table-striped jambo_table bulk_action">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personnel.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_personnel', {'id': personnel.id}) }}"><i class="fa fa-arrow-left fa-2x" aria-hidden="true"></i></a>
<br>
<br>
    <a href="{{ path('edit_personnel', {'id': personnel.id}) }}"><i class="fa fa-pencil-square fa-2x" aria-hidden="true"></i></a>

    {{ include('registrationPersonnel/_delete.html.twig') }}
{% endblock %}
