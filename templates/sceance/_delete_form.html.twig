<form method="post" action="{{ path('sceance_delete', {'id': sceance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sceance.id) }}">
    <button ><i class="fa fa-trash fa-2x" aria-hidden="true"></i></button>
</form>
