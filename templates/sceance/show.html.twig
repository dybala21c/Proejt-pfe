{% extends 'base_admin.html.twig' %}

{% block title %}Sceance{% endblock %}

{% block body %}
    <h1>Sceance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sceance.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ sceance.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sceance_index') }}">Retour</a>

    <a href="{{ path('sceance_edit', {'id': sceance.id}) }}">Modifier</a>

    {{ include('sceance/_delete_form.html.twig') }}
{% endblock %}
