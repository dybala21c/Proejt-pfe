{% extends 'base_admin.html.twig' %}

{% block title %}Sceance{% endblock %}

{% block body %}
    <h1>Sceance</h1>

    <table class="table table-striped jambo_table bulk_action">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sceance.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ sceance.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sceance_index') }}"><i class="fa fa-arrow-left fa-2x" aria-hidden="true"></i></a>

    <a href="{{ path('sceance_edit', {'id': sceance.id}) }}" ><i class="fa fa-pencil-square fa-2x" aria-hidden="true"></i></a>

    {{ include('sceance/_delete_form.html.twig') }}
{% endblock %}
