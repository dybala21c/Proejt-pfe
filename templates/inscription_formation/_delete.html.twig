<form method="post" action="{{ path('inscription_delete', {'id': inscriptionformation.id}) }}" onsubmit="return confirm('tu veux vraiment');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscriptionformation.id) }}">
    <button class="btn">Delete</button>
</form>