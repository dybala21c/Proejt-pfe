{% extends 'base_admin.html.twig' %}

{% block title %}Conge{% endblock %}

{% block body %}
    <h1>Inscrit</h1>

    <table class="table table-striped jambo_table bulk_action">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscriptionformation.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ inscriptionformation.email }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('list_inscription') }}" <i class="fa fa-arrow-left fa-2x" aria-hidden="true"></i></a>
    <br>
    <br>

   {# <a href="{{ path('conge_edit', {'id': conge.id}) }}" <i class="fa fa-pencil-square fa-2x" aria-hidden="true"></i></a> #}

    {{ include('inscription_formation/_delete.html.twig') }}
{% endblock %}