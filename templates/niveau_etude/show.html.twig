{% extends 'base_admin.html.twig' %}

{% block title %}NiveauEtude{% endblock %}

{% block body %}
    <h1>NiveauEtude</h1>

    <table class="table table-striped jambo_table bulk_action">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau_etude.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ niveau_etude.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('niveau_etude_index') }}"><i class="fa fa-arrow-left fa-2x" aria-hidden="true"></i></a>

    <a href="{{ path('niveau_etude_edit', {'id': niveau_etude.id}) }}"><i class="fa fa-pencil-square fa-2x" aria-hidden="true"></i></a>

    {{ include('niveau_etude/_delete_form.html.twig') }}
{% endblock %}
