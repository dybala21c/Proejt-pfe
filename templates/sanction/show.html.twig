{% extends 'base.html.twig' %}

{% block title %}Sanction{% endblock %}

{% block body %}
    <h1>Sanction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sanction.id }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ sanction.Motif }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ sanction.DateDebut ? sanction.DateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ sanction.DateFin ? sanction.DateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sanction_index') }}">back to list</a>

    <a href="{{ path('sanction_edit', {'id': sanction.id}) }}">edit</a>

    {{ include('sanction/_delete_form.html.twig') }}
{% endblock %}
